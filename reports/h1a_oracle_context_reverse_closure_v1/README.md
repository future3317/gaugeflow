# H1a oracle-context coordinate reverse closure

**Status: frozen before execution.** This diagnostic starts from forward-noised
real validation coordinates and applies the exact production quotient reverse
step while holding the clean species and lattice fixed. Those clean inputs make
it an upper-bound field qualification, not a valid generation protocol.

Low/intermediate-noise failure means the learned coordinate vector field itself
is inadequate even with oracle context. Success there combined with failure
from `t=0.999` and the ordinary prior instead attributes the gap to accumulated
rollout-state shift. No new training, repulsion objective, H1b, tensor, or
oracle experiment is authorized here.
