# J1 matched-attribution and gradient synthesis

The two required follow-ups are complete.  They change the causal explanation,
not the historical J1 numbers.

1. The matched C0/C1/C2 Gate fails because the three-clock C2 model is not
   significantly better than single-clock C0 on diagonal or interior states.
   The five-regime task mixture is sufficient to reproduce the J1 noisy/noisy
   endpoint at this budget.  C2 remains useful on clean and element-only
   corners, but cannot be described as the primary repair.
2. The zero-step audit finds norm scaling rather than persistent gradient
   conflict.  Median clip scale is 0.2661 and all pairwise median cosines are
   positive.  The current global clip is retained with no optimizer change.

The least assumptive next step is to freeze E1 and L1 single-head reverse
qualification protocols in the same multimodal state space.  This is protocol
preparation permission only: no element/lattice training begins in this
artifact.  E1/L1 must separately establish categorical chemistry and SPD
lattice reverse closure; only then may M1 expose actual multi-head gradient
interaction, and only a qualified M1 may supply on-policy side states to J2.

No hard chain, extra clock module, clipping variant, ACF, flow matching,
tensor/oracle work, relaxation, DFT or DFPT is authorized.
